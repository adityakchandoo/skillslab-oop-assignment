@{
    ViewBag.Title = "Dash";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>@ViewBag.Title</h1>
                </div>
                <div class="col-sm-6">
                    <a role="button" class="btn btn-primary float-right" href="/Admin/AddTraining">Add Trainings</a>
                </div>
            </div>
        </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">

        <!-- Default box -->
        <div class="card">
            <div class="card-body p-0">
                <table class="table">
                    <thead>
                        <tr>
                            <th style="width: 10px">#</th>
                            <th>Name</th>
                            <th>Description</th>
                            <th>Treshhold</th>
                            <th>Deadline</th>
                            <th>Department</th>
                            <th>Enrolled Count</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var Training in ViewBag.Trainings)
                        {
                            <tr>
                                <td>@Training.Training.TrainingId</td>
                                <td>@Training.Training.Name</td>
                                <td>@Training.Training.Description</td>
                                <td>@Training.Training.Threshold</td>
                                <td>@Training.Training.Deadline</td>
                                <td>@Training.TrainingExtra.DepartmentName</td>
                                <td>@Training.TrainingExtra.NumberOfEmployeesEnrolled</td>
                                <td>
                                    <div class="dropdown">
                                        <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                            <i class="fas fa-wrench"></i>
                                        </button>
                                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                                            <a class="dropdown-item" href="#">View Content</a>
                                            <a class="dropdown-item" href="#">Export Student List</a>
                                        </div>
                                    </div>

                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <!-- /.card -->


    </section>
    <!-- /.content -->
</div>

